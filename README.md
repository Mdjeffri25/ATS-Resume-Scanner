# ATS Resume Scanner

Simple Java project to scan resumes for ATS (Applicant Tracking System) compatibility.

Files:
- `ATSResumeScanner.java` — main source file present in the workspace root.

Usage:
1. Build with your preferred Java tool (javac/IDE/Maven/Gradle).
2. Run the `ATSResumeScanner` class.
